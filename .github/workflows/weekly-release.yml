name: Weekly release

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:
    inputs:
      draft-tags:
        description: >
          Tags or package names to publish, comma-separated.
          Matched as substrings. Empty = all drafts.
        type: string
        default: ''
      dry-run:
        description: Show diff but do not publish.
        type: boolean
        default: false

concurrency:
  group: weekly-release-${{ github.repository }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: read
  issues: write

jobs:
  release:
    uses: gofiber/.github/.github/workflows/weekly-release.yml@main
    with:
      repo-type: single
      dry-run: ${{ inputs.dry-run || false }}
      draft-tags: ${{ inputs.draft-tags || '' }}
